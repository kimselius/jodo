# ============================================================
# Jodo Kernel — Environment Configuration
# ============================================================
# Copy this to .env and fill in your values:
#   cp .env.example .env
#
# This is the ONLY file you need to edit. config.yaml reads
# from these variables — don't edit config.yaml directly.
# ============================================================

# --- Network ---
KERNEL_URL=http://1.2.3.4:8080       # full URL where Jodo can reach this kernel
JODO_IP=5.6.7.8                       # Jodo's VPS IP — kernel SSHes into this

# --- SSH ---
# The kernel SSHes into Jodo's VPS to deploy and manage it.
# Generate a key pair and copy the public key to Jodo's VPS:
#   ssh-keygen -t ed25519 -f ~/.ssh/jodo_vps -N ""
#   ssh-copy-id -i ~/.ssh/jodo_vps.pub root@<JODO_IP>
SSH_KEY_PATH=~/.ssh/jodo_vps
SSH_USER=root

# --- Database ---
JODO_DB_PASSWORD=change-me-to-a-strong-password

# --- LLM API keys (optional — Ollama is free and preferred) ---
CLAUDE_API_KEY=
OPENAI_API_KEY=

# --- Budgets (USD per month) ---
CLAUDE_MONTHLY_BUDGET=20.00
CLAUDE_EMERGENCY_RESERVE=2.00
OPENAI_MONTHLY_BUDGET=10.00
OPENAI_EMERGENCY_RESERVE=1.00

# --- Ollama ---
# If running Ollama on the host (not in Docker), use host.docker.internal
OLLAMA_BASE_URL=http://host.docker.internal:11434
