# ============================================================
# Jodo Kernel — Environment Configuration
# ============================================================
# The easiest way to set up: run ../jodo.sh setup
# Or copy this to .env and fill in values manually.
# ============================================================

# Encryption key for API keys and secrets stored in the database.
# Generate with: openssl rand -base64 32
# DO NOT lose this key — encrypted data becomes unrecoverable without it.
ENCRYPTION_KEY=

# PostgreSQL credentials
DB_HOST=postgres
DB_PORT=5432
DB_USER=jodo
DB_NAME=jodo_kernel
JODO_DB_PASSWORD=

# Everything else (VPS connection, API keys, models, routing, genesis)
# is configured in the web UI after first boot.
